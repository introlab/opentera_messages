syntax = "proto3";

package opentera.protobuf;

message DeviceEvent {

    enum EventType
    {
	DEVICE_UNKNOWN = 0
        DEVICE_CONNECTED = 1;
        DEVICE_DISCONNECTED = 2;
        DEVICE_DELETED = 3;
        DEVICE_ADDED = 4;
    }
    string device_uuid = 1;
    EventType type = 2;
}
